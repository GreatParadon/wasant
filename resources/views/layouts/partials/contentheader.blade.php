<!-- Content Header (Page header) -->
<section class="content-header">
    <h1>
        {{ $page['title'] or 'Page Header' }}
        <small>{{ $page['title'] or 'Page Header' }}</small>
    </h1>
</section>